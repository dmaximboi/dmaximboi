{
  "buildCommand": "npx turbo build --filter=@dmaximboi/web && node -e \"const fs=require('fs');fs.cpSync('apps/web/dist','dist',{recursive:true,force:true})\"",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
